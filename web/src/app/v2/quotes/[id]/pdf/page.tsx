/**
 * Paso 5 · PDF — placeholder.
 *
 * Implementación real (preview + envío + diff drawer + audit trail)
 * en sub-PR `sprint-2/paso-5-pdf`.
 */
export default function PdfPage() {
  return (
    <div className="col placeholder-section">
      <h2 className="font-serif italic" style={{ marginBottom: 8 }}>
        Paso 5 · PDF
      </h2>
      <p className="font-serif italic text-ink-soft">
        Placeholder. Implementación viene en sprint-2/paso-5-pdf.
      </p>
    </div>
  );
}
